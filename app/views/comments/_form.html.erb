<%= form_for([@user, @user.comments.build]) do |f| %>
  <p>
    <%= f.label :roles %><br>
    <%= f.text_field :roles %>
  </p>
   <p>
    <%= f.label :date_of_birth %><br>
    <%= f.text_field :date_of_birth %>
  </p>
  
  <p>
    <%= f.submit %>
  </p>
<% end %>